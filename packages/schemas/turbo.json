{
    "extends": [
        "//"
    ],
    "pipeline": {
        "build": {
            "dependsOn": [
                "backend#build:gql"
            ],
            "outputs": [
                "lib/**"
            ],
            "inputs": [
                "src/**",
                "scripts/**"
            ]
        }
    }
}
